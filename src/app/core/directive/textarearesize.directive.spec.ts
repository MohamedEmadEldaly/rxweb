import { TextarearesizeDirective } from './textarearesize.directive';

describe('TextarearesizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextarearesizeDirective();
    expect(directive).toBeTruthy();
  });
});
